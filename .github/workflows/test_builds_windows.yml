name: Test Build Windows

on:
  push:
    branches:
      - test_build_windows

jobs:
  test_build_windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - name: Test Build Windows
        run: |
          Get-ChildItem